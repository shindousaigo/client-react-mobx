
export namespace Routes {
  export enum Path {
    BASE = '/',
    LOGIN = '/login',
    LAYOUT = '/layout',
    DASHBOARD = '/dashboard',
    SETTING = '/setting'
  }
}

export default class RouteState {
  
}